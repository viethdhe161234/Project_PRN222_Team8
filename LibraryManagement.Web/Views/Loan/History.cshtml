@model IEnumerable<LibraryManagement.Domain.Entities.Loan>

@{
    Layout = "_Layout";
    ViewData["Title"] = "Loan History";
    var returned = Model.Where(l => l.ReturnDate != null).ToList();
    var active = Model.Where(l => l.ReturnDate == null).ToList();
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold">Loan History</h4>
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary">&larr; Current Loans</a>
</div>

<div class="row g-3 mb-4">
    <div class="col-4">
        <div class="card border-0 bg-light shadow-sm text-center" style="border-radius:12px;">
            <div class="card-body py-3">
                <div class="fs-4 fw-bold">@Model.Count()</div>
                <small class="text-muted fw-bold small text-uppercase">Total</small>
            </div>
        </div>
    </div>
    <div class="col-4">
        <div class="card border-0 bg-light shadow-sm text-center" style="border-radius:12px;">
            <div class="card-body py-3">
                <div class="fs-4 fw-bold text-success">@returned.Count</div>
                <small class="text-muted fw-bold small text-uppercase">Returned</small>
            </div>
        </div>
    </div>
    <div class="col-4">
        <div class="card border-0 bg-light shadow-sm text-center" style="border-radius:12px;">
            <div class="card-body py-3">
                <div class="fs-4 fw-bold text-primary">@active.Count</div>
                <small class="text-muted fw-bold small text-uppercase">Active</small>
            </div>
        </div>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info border-0">No loan history found.</div>
}
else
{
    <div class="card border-0 shadow-sm" style="border-radius:15px; overflow:hidden;">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-3">Book</th>
                        <th>Borrow Date</th>
                        <th>Due Date</th>
                        <th>Return Date</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var loan in Model)
                    {
                        <tr>
                            <td class="ps-3">
                                <div class="fw-bold">@loan.BookCopy?.BookEdition?.Book?.Title</div>
                                <small class="text-muted">@loan.BookCopy?.BookEdition?.Publisher</small>
                            </td>
                            <td>@loan.BorrowDate.ToString("dd/MM/yyyy")</td>
                            <td class="@(loan.IsOverdue ? "text-danger fw-bold" : "")">
                                @loan.DueDate.ToString("dd/MM/yyyy")
                            </td>
                            <td>
                                @(loan.ReturnDate.HasValue ? loan.ReturnDate.Value.ToString("dd/MM/yyyy") : "—")
                            </td>
                            <td>
                                @if (loan.ReturnDate.HasValue)
                                {
                                    if (loan.ReturnDate.Value > loan.DueDate)
                                    {
                                        var lateDays = (int)(loan.ReturnDate.Value - loan.DueDate).TotalDays;
                                        <span class="badge bg-warning text-dark">Returned late (@lateDays d)</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">Returned on time</span>
                                    }
                                }
                                else if (loan.IsOverdue)
                                {
                                    <span class="badge bg-danger">Overdue @loan.OverdueDays day(s)</span>
                                }
                                else
                                {
                                    <span class="badge bg-primary">Active</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}